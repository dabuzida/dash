const dartResult = '10D10S#10S';

const strLeng = dartResult.length;
let arrForCheck = [];
let curStr;
for (let i = 0; i < strLeng; i++) {
	curStr = dartResult[i];
	if (typeof curStr === 'number') {
	}

	arrForCheck.push(curStr);
}
